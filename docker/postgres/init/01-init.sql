-- Arquivo legado: atualmente NAO utilizado pelo docker-compose.
-- Banco unico: "identity" (criado por POSTGRES_DB no serviço postgres).
-- Tabelas da aplicacao: criadas pelo Hibernate via spring.jpa.hibernate.ddl-auto.

